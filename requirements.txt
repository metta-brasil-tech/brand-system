# Deps Python pras Vercel serverless functions em api/*.py.
# Versão TRIM do requirements.txt original do ad-generator — só o essencial pra runtime.
# Removidos: click/rich (CLI), fastapi/uvicorn (Vercel não usa servidor), qdrant-client
# (sem Qdrant, skill 02 cai em fallback textual), python-telegram-bot (pertence ao bot do dev),
# litellm (não é importado em nenhum lugar do código -- api/*.py e agente-copy/*.py usam
# anthropic/openai diretamente; litellm sozinho + as deps que ele arrasta (tiktoken,
# tokenizers, huggingface_hub, hf_xet) somavam 152 MB, a causa real do bundle de 354.50 MB
# estourando o limite de 225 MB da Vercel -- confirmado com `pip install --target` local).

openai>=1.50.0
anthropic>=0.40.0
Pillow>=10.4.0
pyyaml>=6.0
jsonschema>=4.23.0
pydantic>=2.9.0
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=8.2.0
