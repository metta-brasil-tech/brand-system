# Deps Python pras Vercel serverless functions em api/*.py.
# Versão TRIM do requirements.txt original do ad-generator — só o essencial pra runtime.
# Removidos: fastapi/uvicorn (Vercel não usa servidor), qdrant-client
# (sem Qdrant, skill 02 cai em fallback textual), python-telegram-bot (pertence ao bot do dev).
#
# ATENÇÃO — litellm é OBRIGATÓRIO, não remover:
#   - api/generate.py:332 instancia LLMAdapter(), e adapters/llm.py levanta
#     ImportError("litellm not installed") sem ele → toda geração falha.
#   - o `click` importado no topo do pipeline.py vem TRANSITIVAMENTE do litellm;
#     removê-lo derruba a geração com "No module named 'click'".
# Removê-lo (commit b4748d1, 09/07) quebrou a produção; revertido aqui.

litellm>=1.50.0
openai>=1.50.0
anthropic>=0.40.0
Pillow>=10.4.0
pyyaml>=6.0
jsonschema>=4.23.0
pydantic>=2.9.0
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=8.2.0
